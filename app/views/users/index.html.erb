<div class="container">
  <h2>ユーザー</h2>
  <% @users.each do |user| %>
    <div class="container-child col-md-2 col-sm-4 col-xs-6">
      <div class="img-child"><%= profile_img(user) %></div>
      <%= link_to user_path(user) do %>
        <h4><strong><%= user.name %></strong></h4>
      <% end %>
      <%= render 'follow_form', user: user %>
    </div>
  <% end %>
</div>
